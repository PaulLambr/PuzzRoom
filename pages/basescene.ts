// basescene.ts
import Phaser from 'phaser';

export default class BaseScene extends Phaser.Scene {
  constructor(key: string) {
    super({ key });
  }

  // Common functions will be added here later
}
